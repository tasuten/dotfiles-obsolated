# 以下自分で追加したもの
# デフォルトで利用可能なのは~/.vim/bundle/neocomplcache/autoload/neocomplcache/sources/snippets_complete/ 以下のファイルを参照

snippet scanf
    scanf("${1}", &${2});${0}

snippet main
	int main(int argc, char *argv[])
	{
		${0}
		return 0;
	}

